function setup() {
    createCanvas(600, 400);
  background("black");}
  
  function draw() {
    
  
  stroke ("red");
  fill ("red");
  
    
    if (mouseIsPressed)
    {circle (mouseX, mouseY, 15);}
  }